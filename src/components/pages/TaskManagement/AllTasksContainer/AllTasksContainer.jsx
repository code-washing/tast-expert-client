import PropTypes from "prop-types";

const AllTasksContainer = ({ allTaskData }) => {
  return <div></div>;
};

AllTasksContainer.propTypes = {
  allTaskData: PropTypes.array,
};

export default AllTasksContainer;
